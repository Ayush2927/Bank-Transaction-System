# Bank-Transaction-System
A production-grade MERN banking engine utilizing an immutable double-entry ledger architecture. Features absolute data consistency via MongoDB ACID transactions, race-condition protection using custom idempotency layers (UUIDv7), and TTL-based JWT token blacklisting for enterprise-level session security.
